scalar DateTime
scalar Date
scalar Time

type Line {
    text: String!
    rowid: Int!
    timestamp: DateTime!
}

type Query {
    numCpus: Int!
    stdin(limit: Int!, offset: Int!, filter: String): [Line!]!
}

type Subscription {
    stdin(filter: String): Line!
}
